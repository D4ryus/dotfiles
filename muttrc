# author: d4ryus https://github.com/d4ryus
# file: ~/.muttrc

source "gpg2 -dq $HOME/.mutt/my_mutt.gpg |"
set folder           = imaps://imap.mail.yahoo.com
set imap_user        = w.wackerbauer@yahoo.de
set imap_pass        = $my_yahoo_pass
set imap_keepalive   = 300
set mail_check       = 120
set smtp_url         = smtps://$imap_user:$my_yahoo_pass@smtp.mail.yahoo.com:465
set editor           = "vim"
set realname         = 'Wolfgang Wackerbauer'
set from             = $imap_user
set use_from         = yes
set ssl_force_tls    = yes
set header_cache     = ~/.mutt/cache
set message_cachedir = ~/.mutt/cache
set mailcap_path     = ~/.mutt/mailcap
mailboxes            = INBOX
set spoolfile        = +INBOX
unset record
unset imap_passive
alternative_order text/palin text/html
auto_view text/html
bind index "u" imap-fetch-mail"
source $HOME/.mutt/my_colorscheme
